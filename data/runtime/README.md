# Runtime Data

This directory stores local runtime state generated on the host/container (for example `active-profile.json`, `op25-status.json`, and transient tails).

These files are intentionally git-ignored and should not be committed.
